import { EmailConfirmationForm } from '../../../features/auth/ui/EmailConfirmationForm'
import { AuthLayout } from '../../../widgets/auth-layout'

const EmailConfirmationPage = () => {
  return (
    <AuthLayout>
      <EmailConfirmationForm />
    </AuthLayout>
  )
}

export default EmailConfirmationPage
